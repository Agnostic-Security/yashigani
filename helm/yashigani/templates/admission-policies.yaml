{{- /* Last updated: 2026-05-17T00:00:00+01:00 */ -}}
{{- /*
YSG-RISK-008 — Kyverno ClusterPolicy admission control.

When admissionPolicies.enabled is true, this template renders six Kyverno
ClusterPolicy resources. Policies 1-5 enforce the container-hardening baseline
for the yashigani namespace; policy 6 (FINDING-V412-UNIVERSAL-003) restricts
PKI trust-plane access from runtime/pool-managed pods. Pods that violate a
policy are REJECTED at admission time (validationFailureAction: Enforce) rather
than discovered post-deployment.

PREREQUISITE: Kyverno must be installed cluster-wide before helm install/upgrade.
Install with: helm install kyverno kyverno/kyverno -n kyverno --create-namespace
The chart does not bundle Kyverno.

NOTE: ClusterPolicy is a cluster-scoped CRD — it does not live inside the
yashigani namespace but it is owned by this Helm release (release name in labels).
Helm will delete these policies on helm uninstall.
*/ -}}
{{- if .Values.admissionPolicies.enabled }}
{{- /*
Pre-install guard: verify that the Kyverno ClusterPolicy CRD is present in the
cluster before attempting to render ClusterPolicy resources. If the CRD is
absent, helm install/upgrade would fail with an opaque "no matches for kind
ClusterPolicy" API-server error. We surface a clear remediation message instead.

lookup returns an empty dict when the object does not exist. A non-empty dict
(i.e. the CRD is registered) allows rendering to proceed normally.
This guard runs during helm template rendering against a live cluster (helm
install/upgrade). It is silently skipped in --dry-run=client mode because
lookup always returns empty in that context (correct: no cluster connection).
*/ -}}
{{- $crd := lookup "apiextensions.k8s.io/v1" "CustomResourceDefinition" "" "clusterpolicies.kyverno.io" }}
{{- if not $crd }}
{{- fail "admissionPolicies.enabled=true but the Kyverno ClusterPolicy CRD is not installed in this cluster.\n\nREMEDIATION: Install Kyverno first, then re-run helm install/upgrade:\n\n  helm repo add kyverno https://kyverno.github.io/kyverno/\n  helm repo update\n  helm install kyverno kyverno/kyverno -n kyverno --create-namespace\n\nWait for Kyverno to become ready before deploying Yashigani:\n  kubectl wait --for=condition=ready pod -l app.kubernetes.io/instance=kyverno -n kyverno --timeout=120s\n\nThen re-run your helm install/upgrade command.\nAlternatively, set --set admissionPolicies.enabled=false to install without Kyverno (document in risk register per YSG-RISK-008)." }}
{{- end }}
---
# Policy 1/6: require runAsNonRoot
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: {{ .Release.Name }}-require-run-as-non-root
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
    app.kubernetes.io/component: admission-policy
  annotations:
    policies.kyverno.io/title: "Require runAsNonRoot"
    policies.kyverno.io/category: "Pod Security"
    policies.kyverno.io/severity: "high"
    policies.kyverno.io/subject: "Pod"
    policies.kyverno.io/description: >-
      YSG-RISK-008: Pods in the yashigani namespace must set runAsNonRoot: true
      at pod or container level, or provide a non-zero runAsUser.
spec:
  validationFailureAction: {{ .Values.admissionPolicies.validationFailureAction }}
  background: true
  rules:
    - name: check-containers-run-as-non-root
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      validate:
        message: >-
          YSG-RISK-008: Pod must set runAsNonRoot: true or a non-zero runAsUser
          at the pod level. Pod: {{ "{{" }} request.object.metadata.name {{ "}}" }}.
        deny:
          conditions:
            all:
              - key: "{{ "{{" }} request.object.spec.securityContext.runAsNonRoot || false {{ "}}" }}"
                operator: Equals
                value: false
              - key: "{{ "{{" }} request.object.spec.securityContext.runAsUser || `0` {{ "}}" }}"
                operator: Equals
                value: 0
---
# Policy 2/6: require allowPrivilegeEscalation: false
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: {{ .Release.Name }}-require-no-privilege-escalation
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
    app.kubernetes.io/component: admission-policy
  annotations:
    policies.kyverno.io/title: "Require allowPrivilegeEscalation false"
    policies.kyverno.io/category: "Pod Security"
    policies.kyverno.io/severity: "high"
    policies.kyverno.io/subject: "Pod"
    policies.kyverno.io/description: >-
      YSG-RISK-008: All containers must set allowPrivilegeEscalation: false.
spec:
  validationFailureAction: {{ .Values.admissionPolicies.validationFailureAction }}
  background: true
  rules:
    - name: check-privilege-escalation
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      validate:
        message: >-
          YSG-RISK-008: All containers in pod
          '{{ "{{" }} request.object.metadata.name {{ "}}" }}' must explicitly set
          allowPrivilegeEscalation: false. Omitting the field is not permitted
          (K8s defaults to true when absent).
        foreach:
          - list: "request.object.spec.containers"
            deny:
              conditions:
                any:
                  - key: "{{ "{{" }} element.securityContext.allowPrivilegeEscalation {{ "}}" }}"
                    operator: NotEquals
                    value: false
---
# Policy 3/6: require seccomp RuntimeDefault or Localhost
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: {{ .Release.Name }}-require-seccomp-profile
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
    app.kubernetes.io/component: admission-policy
  annotations:
    policies.kyverno.io/title: "Require seccomp RuntimeDefault or Localhost"
    policies.kyverno.io/category: "Pod Security"
    policies.kyverno.io/severity: "medium"
    policies.kyverno.io/subject: "Pod"
    policies.kyverno.io/description: >-
      YSG-RISK-008: Pods must specify seccompProfile.type of RuntimeDefault
      or Localhost at the pod level.
spec:
  validationFailureAction: {{ .Values.admissionPolicies.validationFailureAction }}
  background: true
  rules:
    - name: check-seccomp-profile
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      validate:
        message: >-
          YSG-RISK-008: Pod '{{ "{{" }} request.object.metadata.name {{ "}}" }}'
          must set seccompProfile.type to RuntimeDefault or Localhost.
          Unconfined is not permitted.
        deny:
          conditions:
            all:
              - key: "{{ "{{" }} request.object.spec.securityContext.seccompProfile.type || 'Unconfined' {{ "}}" }}"
                operator: AnyIn
                value:
                  - Unconfined
                  - ""
---
# Policy 4/6: require capabilities.drop: [ALL]
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: {{ .Release.Name }}-require-drop-all-capabilities
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
    app.kubernetes.io/component: admission-policy
  annotations:
    policies.kyverno.io/title: "Require drop ALL capabilities"
    policies.kyverno.io/category: "Pod Security"
    policies.kyverno.io/severity: "high"
    policies.kyverno.io/subject: "Pod"
    policies.kyverno.io/description: >-
      YSG-RISK-008: All containers must set capabilities.drop containing ALL.
spec:
  validationFailureAction: {{ .Values.admissionPolicies.validationFailureAction }}
  background: true
  rules:
    - name: check-capabilities-drop-all
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      validate:
        message: >-
          YSG-RISK-008: All containers in pod
          '{{ "{{" }} request.object.metadata.name {{ "}}" }}' must set
          capabilities.drop containing ALL.
        foreach:
          - list: "request.object.spec.containers"
            deny:
              conditions:
                all:
                  - key: "ALL"
                    operator: AnyNotIn
                    value: "{{ "{{" }} element.securityContext.capabilities.drop[] || `[]` {{ "}}" }}"
---
# Policy 5/6: restrict runAsUser: 0
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: {{ .Release.Name }}-restrict-root-user
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
    app.kubernetes.io/component: admission-policy
  annotations:
    policies.kyverno.io/title: "Restrict explicit root user (UID 0)"
    policies.kyverno.io/category: "Pod Security"
    policies.kyverno.io/severity: "critical"
    policies.kyverno.io/subject: "Pod"
    policies.kyverno.io/description: >-
      YSG-RISK-008: Pods must not set runAsUser: 0 at pod or container level.
spec:
  validationFailureAction: {{ .Values.admissionPolicies.validationFailureAction }}
  background: true
  rules:
    - name: restrict-root-pod-level
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      validate:
        message: >-
          YSG-RISK-008: Pod '{{ "{{" }} request.object.metadata.name {{ "}}" }}'
          sets runAsUser: 0 at pod level. Root containers are not permitted.
        deny:
          conditions:
            all:
              - key: "{{ "{{" }} request.object.spec.securityContext.runAsUser || `1` {{ "}}" }}"
                operator: Equals
                value: 0
    - name: restrict-root-container-level
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      validate:
        message: >-
          YSG-RISK-008: All containers in pod
          '{{ "{{" }} request.object.metadata.name {{ "}}" }}' must not set
          runAsUser: 0. Root containers are not permitted.
        foreach:
          - list: "request.object.spec.containers"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.securityContext.runAsUser || `1` {{ "}}" }}"
                    operator: Equals
                    value: 0
---
# Policy 6/6: restrict PKI trust-plane access (FINDING-V412-UNIVERSAL-003, CRITICAL)
#
# The gateway ServiceAccount holds pods:create in-namespace (YSG-RISK-070,
# rbac-pool-manager.yaml) so it can spawn per-identity pool pods. Policies 1-5
# stop those pods being PRIVILEGED, but NONE of them stop a compromised gateway
# from creating a Pod that (a) assumes the yashigani-pki ServiceAccount — which
# can read/update the CA private-key Secret — or (b) mounts/references that
# CA-keys Secret directly (volume / projected-source / env). Either = full mTLS
# trust-plane forgery.
#
# SCOPING (Nico+Captain CRITICAL-1): the deny is gated on the NON-FORGEABLE
# authenticated caller identity (request.userInfo.username), NOT on client-
# supplied object metadata. An earlier draft exempted pods by
# ownerReferences[0].kind != Job, but K8s does not validate submitted
# ownerReferences — the gateway SA could forge ownerReferences:[{kind:Job}] on a
# bare pod and bypass every rule. These rules instead fire when the pod's CREATOR
# (request.userInfo.username, set by the API server from the bearer token — not
# spoofable) matches the gateway ServiceAccount. The precondition uses AnyIn over
# BOTH (a) the chart's SA-name helper — the gateway's ACTUAL runtime identity,
# which tracks a serviceAccount.name override (IRSA/Workload-Identity); a hardcoded
# literal would be silently defeated by that override (Captain R2 HIGH) — and (b)
# the literal `yashigani` that rbac-pool-manager.yaml hard-binds pods:create to, so
# the pods:create holder is always covered even if it diverges from the gateway SA.
# Legit PKI pods (yashigani-pki-bootstrap Job, mtls-rotation CronJob) are created by
# the kube-system controllers, never by these identities, so they are never matched.
# INVARIANT: only the gateway SA holds pods:create in-ns (verified
# rbac-pool-manager.yaml + extractor-job-template.yaml). NOTE (chart wart, out of
# this finding's scope, flagged for the team): rbac-pool-manager binds pods:create
# to literal `yashigani` while gateway.yaml runs under the helper — under a
# serviceAccount.name override these DIVERGE and the pool backend loses pods:create
# (functional bug); the AnyIn set here keeps policy 6 safe regardless.
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: {{ .Release.Name }}-restrict-pki-trust-plane
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
    app.kubernetes.io/component: admission-policy
  annotations:
    policies.kyverno.io/title: "Restrict PKI trust-plane access (UNIVERSAL-003)"
    policies.kyverno.io/category: "Privilege Escalation"
    policies.kyverno.io/severity: "critical"
    policies.kyverno.io/subject: "Pod"
    policies.kyverno.io/description: >-
      FINDING-V412-UNIVERSAL-003: deny the gateway ServiceAccount from creating
      Pods that assume the yashigani-pki ServiceAccount or mount/reference the CA
      private-key Secret (volume/projected/env), closing the gateway pods:create
      -> CA-key -> mTLS-forgery path.
    yashigani.io/finding: "FINDING-V412-UNIVERSAL-003"
    yashigani.io/risk-id: "YSG-RISK-070"
spec:
  validationFailureAction: {{ .Values.admissionPolicies.validationFailureAction }}
  # background: false — request.userInfo (the gate below) is only present in the
  # admission request, not in background scans of pre-existing resources.
  background: false
  rules:
    # Rule 1: the gateway SA may not create a Pod that assumes yashigani-pki.
    - name: deny-assume-pki-sa
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      preconditions:
        all:
          # NON-FORGEABLE gate — the authenticated creator identity (see header).
          - key: "{{ "{{" }} request.userInfo.username {{ "}}" }}"
            operator: AnyIn
            value:
              # The gateway's ACTUAL runtime identity (tracks a serviceAccount.name
              # override, e.g. IRSA/Workload-Identity) — gateway.yaml uses this helper.
              - "system:serviceaccount:{{ .Release.Namespace }}:{{ include "yashigani.serviceAccountName" . }}"
              # The literal SA that rbac-pool-manager.yaml hard-binds pods:create to
              # (unchanged by the override) — belt-and-braces so the pods:create holder
              # is always covered even if it diverges from the gateway identity.
              - "system:serviceaccount:{{ .Release.Namespace }}:yashigani"
      validate:
        message: >-
          FINDING-V412-UNIVERSAL-003: the gateway ServiceAccount may not create a
          Pod ('{{ "{{" }} request.object.metadata.name {{ "}}" }}') that assumes the
          privileged 'yashigani-pki' ServiceAccount.
        deny:
          conditions:
            all:
              - key: "{{ "{{" }} request.object.spec.serviceAccountName || 'default' {{ "}}" }}"
                operator: Equals
                value: yashigani-pki
    # Rule 2: the gateway SA may not create a Pod that mounts the CA-keys Secret
    # as a volume — direct secret volume OR projected secret source.
    - name: deny-mount-ca-keys-secret
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      preconditions:
        all:
          - key: "{{ "{{" }} request.userInfo.username {{ "}}" }}"
            operator: AnyIn
            value:
              # The gateway's ACTUAL runtime identity (tracks a serviceAccount.name
              # override, e.g. IRSA/Workload-Identity) — gateway.yaml uses this helper.
              - "system:serviceaccount:{{ .Release.Namespace }}:{{ include "yashigani.serviceAccountName" . }}"
              # The literal SA that rbac-pool-manager.yaml hard-binds pods:create to
              # (unchanged by the override) — belt-and-braces so the pods:create holder
              # is always covered even if it diverges from the gateway identity.
              - "system:serviceaccount:{{ .Release.Namespace }}:yashigani"
      validate:
        message: >-
          FINDING-V412-UNIVERSAL-003: the gateway ServiceAccount may not create a
          Pod ('{{ "{{" }} request.object.metadata.name {{ "}}" }}') that mounts the CA
          private-key Secret '{{ .Values.mtls.caKeysSecretName }}'.
        foreach:
          # Direct secret volume (null-safe list; a Pod may have no volumes).
          - list: "request.object.spec.volumes[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.secret.secretName || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
          # CRITICAL-2 fix (Nico+Captain): projected secret sources use a
          # different field path (element.secret.name); the direct-volume check
          # above is null for a projected volume, so cover it explicitly.
          - list: "request.object.spec.volumes[].projected.sources[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.secret.name || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
    # Rule 3: the gateway SA may not create a Pod that references the CA-keys
    # Secret via env (secretKeyRef) / envFrom (secretRef) — containers,
    # initContainers, and ephemeralContainers (defense-in-depth; the gateway SA
    # lacks pods/ephemeralcontainers per rbac-pool-manager.yaml, but cover it).
    - name: deny-env-ca-keys-secret
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaces:
                - {{ .Release.Namespace }}
      preconditions:
        all:
          - key: "{{ "{{" }} request.userInfo.username {{ "}}" }}"
            operator: AnyIn
            value:
              # The gateway's ACTUAL runtime identity (tracks a serviceAccount.name
              # override, e.g. IRSA/Workload-Identity) — gateway.yaml uses this helper.
              - "system:serviceaccount:{{ .Release.Namespace }}:{{ include "yashigani.serviceAccountName" . }}"
              # The literal SA that rbac-pool-manager.yaml hard-binds pods:create to
              # (unchanged by the override) — belt-and-braces so the pods:create holder
              # is always covered even if it diverges from the gateway identity.
              - "system:serviceaccount:{{ .Release.Namespace }}:yashigani"
      validate:
        message: >-
          FINDING-V412-UNIVERSAL-003: the gateway ServiceAccount may not create a
          Pod ('{{ "{{" }} request.object.metadata.name {{ "}}" }}') that references the
          CA private-key Secret '{{ .Values.mtls.caKeysSecretName }}' via env/envFrom.
        foreach:
          - list: "request.object.spec.containers[].env[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.valueFrom.secretKeyRef.name || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
          - list: "request.object.spec.containers[].envFrom[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.secretRef.name || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
          - list: "request.object.spec.initContainers[].env[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.valueFrom.secretKeyRef.name || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
          - list: "request.object.spec.initContainers[].envFrom[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.secretRef.name || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
          - list: "request.object.spec.ephemeralContainers[].env[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.valueFrom.secretKeyRef.name || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
          - list: "request.object.spec.ephemeralContainers[].envFrom[] || `[]`"
            deny:
              conditions:
                all:
                  - key: "{{ "{{" }} element.secretRef.name || '' {{ "}}" }}"
                    operator: Equals
                    value: {{ .Values.mtls.caKeysSecretName | quote }}
{{- end }}
