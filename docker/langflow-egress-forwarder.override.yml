# GENERATED BY yashigani codegen — DO NOT EDIT MANUALLY
# .yashigani-manifest-hash: e121aa16061ccbee
# YSG_RUNTIME: docker
# Editing this file manually breaks drift detection (M9).
# Re-generate with: yashigani onboard <manifest.yaml>

# v4.1 unified-sidecar Phase 2a — egress-forwarder override for system: langflow (tenant: default)
# Declared egress prefixes: llm
#
# SPLIT RINGFENCE TOPOLOGY (design §2.6 — BINDING, I1-02 preservation):
#   ringfence_langflow_in = EXACTLY {langflow, caddy}       — ingress zone, 2-member
#   ringfence_langflow_eg = EXACTLY {langflow, egress-langflow} — egress zone, 2-member
# The forwarder is NEVER on the ingress ringfence: a compromised forwarder
# must have no L3 path to the shim's plaintext port. Enforced structurally
# by codegen.validate_ringfence_topology() — 3rd member = CodegenError.
#
# INTRA-ZONE-DENY (§2.2(d)): egress-langflow → langflow:7860 REFUSED.
# Mechanism: ringfence-init in the FORWARDER netns applies default-deny
# OUTPUT permitting ONLY caddy:18790 (+DNS +ESTABLISHED). Netns-equivalent
# of the design's bridge FORWARD DROP (containers cannot program the host
# bridge FORWARD chain). Also enforces synthesis #2: no forwarder path to
# gateway:8080 / backoffice:8443 despite caddy_internal membership.
# Live REFUSED probe = Phase-2b acceptance gate (§8 item 4, Laura co-sign).
services:
  langflow:
    networks:
      - ringfence_langflow_in
      - ringfence_langflow_eg
  # Ingress front: caddy joins the INGRESS ringfence only (sole co-member).
  # Compose merges this list with the base caddy networks (additive).
  caddy:
    networks:
      - ringfence_langflow_in
  # The egress-forwarder sidecar (§2.3) — sole holder of the SVID mount;
  # presents the leaf to caddy:18790; listens plain-HTTP :9400 on the
  # egress ringfence. NO CADDY_INTERNAL_HMAC is mounted or injected here.
  egress-langflow:
    image: ${YASHIGANI_EGRESS_FORWARDER_IMAGE:-yashigani/caddy}:${YASHIGANI_VERSION:-3.0.0}
    restart: unless-stopped
    # Plain `caddy run` — caddy-entrypoint.sh (iptables logic, needs
    # NET_ADMIN) deliberately bypassed; every capability is dropped.
    entrypoint: ["caddy"]
    command: ["run", "--config", "/etc/caddy/egress-forwarder.caddy", "--adapter", "caddyfile"]
    environment:
      # Caddy writable state on tmpfs (read_only rootfs).
      XDG_CONFIG_HOME: /tmp/caddy/config
      XDG_DATA_HOME: /tmp/caddy/data
    expose:
      - "9400"
    volumes:
      - ./caddy/egress/langflow-forwarder.caddy:/etc/caddy/egress-forwarder.caddy:ro
      # TRANSITIONAL (spec.egress.svid_files — retires at per-instance
      # migration, design §3.1 step 1): hand-minted service leaf bound at
      # the STANDARD SVID paths so the forwarder config is mode-agnostic.
      # FORWARDER-ONLY mount of this material (synthesis #2).
      - ./secrets/langflow_client.crt:/run/secrets/svid/default/langflow/client.crt:ro
      - ./secrets/langflow_client.key:/run/secrets/svid/default/langflow/client.key:ro
      - ./secrets/ca_root.crt:/run/secrets/svid/default/langflow/ca.crt:ro
    tmpfs:
      - /tmp:size=16m,mode=0700
    networks:
      # Egress ringfence (2-member: langflow + this forwarder) + caddy_internal
      # to reach caddy:18790. L1 default-deny (init below) pins caddy:18790 as
      # the ONLY initiable destination.
      - ringfence_langflow_eg
      - caddy_internal
    user: "1000:1000"  # matches the transitional key file owner (0600)
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    # L3 — IPv6 default-deny
    sysctls:
      net.ipv6.conf.all.disable_ipv6: 1
      net.ipv6.conf.default.disable_ipv6: 1
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:9400/healthz >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
        reservations:
          cpus: '0.05'
          memory: 32M
  # INTRA-ZONE-DENY enforcer: runs in the FORWARDER's netns (network_mode
  # service:), applies default-deny OUTPUT + ACCEPT caddy:18790 + DNS +
  # ESTABLISHED (inbound langflow→forwarder:9400 responses still flow).
  # DENIES: egress-langflow → langflow:7860 (and every other destination).
  #
  # ORDERING (Captain): network_mode service:<forwarder> already implies
  # init-after-forwarder; the reverse depends_on (forwarder waits for
  # init completion, the classic L7 pattern) is a dependency CYCLE that
  # compose v5 rejects outright. The rules therefore land moments after
  # the forwarder process starts. Accepted: the deny is belt-and-
  # suspenders against a COMPROMISED forwarder (a post-boot event); the
  # forwarder never legitimately initiates to the system; and the
  # Phase-2b live acceptance gate (§8 item 4) probes the APPLIED state.
  ringfence-init-egress-langflow:
    image: ${YASHIGANI_RINGFENCE_INIT_IMAGE:-ghcr.io/agnosticsec/ringfence-init}:${YASHIGANI_RINGFENCE_INIT_VERSION:-0.1.0}
    network_mode: "service:egress-langflow"
    depends_on:
      egress-langflow:
        condition: service_started
    cap_drop:
      - ALL
    cap_add:
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      RINGFENCE_CADDY_HOST: caddy
      RINGFENCE_CADDY_PORT: "18790"
      RINGFENCE_DNS_SERVER: "127.0.0.11"
      RINGFENCE_RUNTIME: "docker"
      RINGFENCE_AGENT_NAME: "egress-langflow"
    tmpfs:
      - /run/ringfence:size=1m,mode=1777
# SPLIT RINGFENCES (§2.6): both internal:true, IPv6 off. Membership is the
# invariant — validate_ringfence_topology() errors on any 3rd member.
networks:
  ringfence_langflow_in:
    driver: bridge
    enable_ipv6: false
    internal: true
  ringfence_langflow_eg:
    driver: bridge
    enable_ipv6: false
    internal: true
